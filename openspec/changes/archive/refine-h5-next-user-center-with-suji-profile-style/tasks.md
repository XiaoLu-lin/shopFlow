# 任务：refine-h5-next-user-center-with-suji-profile-style

## 1. 个人中心 A 方案建档

- [x] 确认个人中心 UI 方向为本地预览中的 `A. 原版 Suji 感`。
- [x] 创建 proposal、design、tasks 和配套设计稿、实施计划。
- [x] 明确这轮只改个人中心，不回改其他交易页。

验收契约：

- proposal MUST 明确本次不修改后端接口和其他交易页。
- design SHALL 明确页面采用“渐变头部 + 订单宫格 + 列表式服务入口”的结构。

## 2. 个人中心展示逻辑与页面改造

- [x] 先为头部文案、手机号脱敏和服务入口排序补失败测试。
- [x] 改造 `src/pages/user/index.vue` 为 A 方案结构。
- [x] 保留设置、全部订单、购物车、收藏、地址、优惠券、售后、帮助、服务等现有入口。

验收契约：

- 个人中心 MUST 保留现有服务入口与跳转能力。
- 页面 SHALL 更接近 `docs-suji` Profile 的结构，而不是继续使用双列说明卡。
- 页面不得伪造用户 ID、会员等级、积分等当前无真实字段的数据。

## 3. 验证与 review

- [x] 运行个人中心相关测试。
- [x] 运行 `type-check`。
- [x] 在本地 H5 个人中心页做视觉检查。
- [x] 创建 review 记录本次覆盖范围与残余风险。

验收契约：

- 在未提供最新测试和 `type-check` 证据前，不得声称本次完成。
- review MUST 记录本次只处理个人中心页，其他页面留待后续批次处理。
