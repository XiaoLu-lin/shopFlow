# Tasks: align-litemall-vue-tenant-user-headers

## 1. OpenSpec

- [x] 明确旧 `litemall-vue` 与当前 ShopFlow 用户侧协议在租户头、用户头、首页初始化和鉴权语义上的差异。
- [x] 将当前 change 从“最小租户兼容”扩展为“旧前台鉴权整体兼容”，同步 proposal、design、tasks、spec 与 review。

## 2. 前台租户初始化

- [x] 为 `litemall-vue` 增加前台 `appid` 配置读取入口，并明确开发联调默认值。
- [x] 调整首页初始化请求，使其显式携带 `appid` 并接收首页返回中的租户 token。
- [x] 将租户 token 保存到前台本地存储，作为后续请求的统一来源。
- [x] 扩展租户预热白名单，使登录、注册、验证码、密码重置等鉴权页请求也能复用同一套租户预热逻辑。

## 3. 请求头兼容

- [x] 更新 `litemall-vue` 统一请求封装，停止默认发送 `X-Litemall-Token`。
- [x] 将本地 `Authorization` 映射为 `X-ShopFlow-User-Token`。
- [x] 将本地租户 token 映射为 `X-ShopFlow-TenantId`。
- [x] 保证首页初始化白名单请求在首包阶段不会因为缺少租户头而被错误阻塞。
- [x] 对登录成功返回的用户 token 做统一归一与持久化，避免页面各自处理 `token/userToken` 差异。

## 4. 后端旧 H5 登录兼容

- [x] 在 `shopflow-wx-api` 新增面向旧 H5 的普通用户账号登录兼容入口。
- [x] 复用现有用户查询、密码校验、token 签发与用户状态校验逻辑，避免破坏当前管理员绑定流程。
- [x] 明确兼容支持的账号类型与错误提示，避免旧邮箱分支落入误导性授权错误。
- [x] 补充最小验证，确认小程序原有 `/wx/auth/login` 语义未被影响。

## 5. 前台鉴权页兼容

- [x] 调整 `litemall-vue` 登录页，改用新的普通用户登录兼容入口并对齐成功返回结构。
- [x] 调整注册验证码接口映射，从旧 `/auth/regCaptcha` 对齐到当前短信验证码接口。
- [x] 调整注册提交参数，只发送当前后端实际需要的字段，同时保留前端基础校验。
- [x] 补齐找回密码页面的验证码发送、路由传参和重置提交逻辑。
- [x] 复核用户中心改密、改手机号等复用验证码逻辑的页面，保证接口映射一致。

## 6. 联调与复核

- [x] 验证首页进入链路不再出现“未找到授权租户”。
- [x] 验证首页成功后本地已写入租户 token。
- [x] 验证至少一条依赖租户上下文的兼容接口请求可正常携带双 header。
- [x] 验证旧 H5 普通用户登录成功后，前台已写入可被请求层识别的用户 token。
- [x] 验证旧 H5 注册流程可完成验证码发送与注册提交。
- [x] 验证旧 H5 找回密码流程可完成验证码发送与密码重置。
- [x] 在 review 中分别记录已完成兼容项、遗留接口差异和未纳入本次范围的历史分支。
