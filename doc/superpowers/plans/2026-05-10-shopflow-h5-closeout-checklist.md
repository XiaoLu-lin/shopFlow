# ShopFlow H5 重构收尾清单

## 0. 暂停说明

- 当前 `shopflow-h5-next` 主体迁移已基本完成。
- 按当前安排，先暂停 H5 重构继续开发，把未完成事项留档，等待更高优先级任务处理完后再回到本项目。
- 本文档作为当前阶段的收尾与恢复清单，后续继续时优先从 `P0 / P1` 项开始。

## 1. 当前已具备的收口基础

- `shopflow-h5-next` 已完成 `Vue 3 + TypeScript + Vite 6 + Vant 4 + Tailwind 3.4 + Pinia + Vue Query` 基础工程搭建。
- 兼容层已覆盖旧登录态、本地存储键、双 header、tenant bootstrap、hash 路由语义。
- 浏览域、鉴权域、交易域、用户域主页面已迁移完成，且主链路页面可以实际运行。
- 售后链路已具备列表、详情、申请、撤销、凭证上传能力。
- 单元测试、构建、Playwright Chromium/WebKit smoke 已可执行。
- GitHub Actions 已包含 `build-h5-next` 构建与测试步骤。

## 2. 可以直接收口的任务

- 同步 `openspec/changes/rebuild-shopflow-h5-modern-spa-with-tailwind/tasks.md` 的已完成项。
- 更新 `openspec/changes/rebuild-shopflow-h5-modern-spa-with-tailwind/review.md`，写入已完成范围。
- 补齐 review 中的残余风险与切流前置条件。
- 复核页面矩阵、设计稿、实施计划与当前代码状态是否一致。

## 3. 未完成事项总览

### P0：恢复开发后优先处理

- 同步 `tasks.md`
  - 现状：OpenSpec 任务仍全部未勾选，但代码实现已完成大半。
  - 原因：文档状态与真实代码状态不一致，后续继续开发前容易失真。
- 更新 `review.md`
  - 现状：`review.md` 仍是占位内容。
  - 原因：残余风险、当前覆盖范围、切流前置条件尚未正式沉淀。
- 补交易链路更完整 E2E
  - 已有：购物车 -> 结算 -> 支付结果 smoke。
  - 未有：商品详情加购、立即购买、支付失败/取消、优惠券切换、登录拦截 redirect。
- 明确切流与回滚方案
  - 现状：新旧站并行可行，但入口切换、回滚步骤、最终验收门槛未正式记录。

### P1：功能缺口

- 支付宝支付链路未接通
  - 当前状态：支付页有入口，但仍为占位说明。
- `ServicePage` 仍是静态内容
  - 当前状态：可访问，但不是最终真实业务数据页。
- 微信内 H5 手工回归未落结果
  - 当前状态：自动化已覆盖 Chromium/WebKit smoke，但微信容器特有链路还未沉淀验证记录。

### P1：测试缺口

- 关键组件测试未补齐
  - 登录表单
  - 地址编辑
  - SKU / 规格选择
  - 购物车结算块
  - 订单筛选
  - 空状态 / 骨架屏 / 错误态
- 鉴权相关 E2E 仍不完整
  - 登录
  - 注册
  - 找回密码
  - 退出登录

### P2：可延后优化项

- 售后申请页上传后当前为轻量文案块展示，不是图片缩略图。
- 个别说明文案仍偏开发态，可在最终验收前统一收口。
- 可继续压缩少量过渡性样式和说明型 UI 提示。

## 4. 仍需继续完成的功能项

### 3.1 业务功能

- 支付宝支付链路仍未接通，支付页当前仍为占位入口。
- `ServicePage` 仍为静态内容，尚未接入真实服务数据或运营配置。
- 交易链路虽已具备 smoke，但仍缺少更深层回归：
  - 商品详情加购
  - 商品详情立即购买
  - 购物车编辑/删除
  - 优惠券切换影响结算
  - 支付取消/失败分支

### 3.2 验证覆盖

- 组件测试尚未系统补齐，提案中的关键组件仍缺：
  - 登录表单
  - 地址编辑
  - SKU / 规格选择
  - 购物车结算块
  - 订单筛选
  - 空状态 / 骨架屏 / 错误态
- E2E 目前已有 smoke，但还未覆盖完整矩阵：
  - 登录
  - 注册
  - 找回密码
  - 加购
  - 结算
  - 支付结果全分支
  - 地址管理
  - 个人资料修改
  - 退出登录
- 微信内 H5 的手工回归仍未沉淀结果：
  - 登录
  - 支付回跳
  - 分享打开链路

## 5. 切流前必须完成的检查

- 明确旧站与新站静态入口切换方式。
- 明确回滚方案：
  - 切回旧 `shopflow-h5`
  - 保留旧 hash 分享链接可继续访问
- 确认 `/wx/*` 接口契约未发生结构性漂移。
- 确认本地存储兼容键在新旧站之间可以相互读写。
- 确认后端 `6914` 联调环境下首页预热、登录、结算、支付、售后链路至少人工回归一遍。

## 6. 推荐恢复顺序

### 第一阶段：恢复开发先做

1. 更新 OpenSpec `tasks.md`
2. 更新 OpenSpec `review.md`
3. 补交易链路 E2E 缺口
4. 明确切流与回滚方案

### 第二阶段：补功能缺口

1. 更新 OpenSpec `tasks.md` 与 `review.md`
2. 补关键组件测试
3. 补交易与鉴权 E2E 矩阵
4. 评估是否接支付宝支付
5. 补 `ServicePage` 真实化
6. 做一轮微信内 H5 手工回归
7. 最终确认切流与回滚方案

## 7. 当前建议

- 当前暂停是合理的，因为项目已经越过“从 0 到 1”阶段，剩余工作大多属于验证、切流和少量尾项。
- 后续恢复时，不建议直接继续堆页面，应该先把 OpenSpec 文档状态与验证状态补齐，再继续功能收口。
